<div id="rota">
  <% if @rota.empty? %>
    <p>Nenhuma rota registrada</p>
  <%else%>
    <h1>Rotas de <%= @motorista.nome %>, CPF: <%= @motorista.cpf %></h1>
    <% @rota.each do |rotum| %>
      <%= render rotum %>
    <% end %>
  <% end %>
</div>